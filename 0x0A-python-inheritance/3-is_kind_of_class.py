#!/usr/bin/python3
"""
The is_kind_of_class function is inside
"""


def is_kind_of_class(obj, a_class):
    """True if obj is an instance or inherited else return False"""
    return (isinstance(obj, a_class))
